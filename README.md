# CD-LAM
CD-LAM is a LAM-side causal debiasing method for continuous latent actions. CD-LAM keeps the downstream action conditioning format unchanged while debiasing the latent action space before ACWM training, combining embodiment-centric reconstruction, action-centric contrastive learning, and latent space calibration. 
